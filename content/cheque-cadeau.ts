/**
 * PAGE CHÈQUE CADEAU
 * ===================
 * Passe `periodeCadeaux` à `true` en novembre-décembre pour mettre la
 * page en avant (bandeau, ton plus festif) — remets `false` le reste de
 * l'année. Les montants sont des exemples, à confirmer.
 */

export const periodeCadeaux = false;

export const montantsExemple = ["30 €", "50 €", "80 €", "Montant libre"];

export const montantsSontExemple = true;
